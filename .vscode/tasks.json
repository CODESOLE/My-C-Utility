{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "clang -g ${file} -o ${fileBasenameNoExtension}.out "
        }
    ]
}